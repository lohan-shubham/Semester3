I prefer to use real mode(16-bit mode) because we have very low memory in our system 
if we try to use long mode then its consume lots of memory (becasue it scaled to 64 bit size) even for minor task as compare to real mode.

